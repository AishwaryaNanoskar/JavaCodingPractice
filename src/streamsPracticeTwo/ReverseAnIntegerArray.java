package streamsPracticeTwo;

import java.util.Arrays;
import java.util.List;

public class ReverseAnIntegerArray {
	public static void main(String[] args) {
		List<Integer> list = Arrays.asList(4, 5, 7, 3, 9, 10);
		
//		List<Integer> reverse = list.stream().reverseOrder().collect(Collectors.toList());
		
//		IntStream.rangeClosed(0, list).map(i -> l)
		
		
	}
}
